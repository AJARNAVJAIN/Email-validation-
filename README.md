

# Email Validation using Python (Regex)

This project demonstrates how to validate email addresses in Python using **Regular Expressions (Regex)**.  
It checks whether an email follows a standard format such as `username@domain.extension`.

---

## 📌 Features

- Validates email addresses using Python's built-in `re` module
- Supports:
  - Lowercase letters
  - Numbers
  - Dots (`.`), underscores (`_`), plus (`+`), and hyphens (`-`)
- Works with multi-level domains like:
  - `company.co.in`
  - `iitb.ac.in`
  - `sub.domain.com`

---

## 🛠️ Technologies Used

- **Python 3**
- **Regular Expressions (re module)**

---

## 📂 Project Structure

